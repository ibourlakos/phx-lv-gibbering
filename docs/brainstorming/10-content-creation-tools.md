Placeholder for content creation tools — both support-editor (official catalogue) and player-facing (UGC library).

Shared surface: content-type schemas, validation logic, core editor components (item editor, race editor, map module editor, etc.), preview rendering.

Divergent surface: the shell around those components differs between the admin panel (platform-scoped, no ownership, bulk ops, immediate publish) and the game app (user-owned, visibility controls, submission/review workflow).

See brainstorm [#09](09-admin-web-app.md) for the admin panel context and the UGC moderation model (trust levels, content status lifecycle).
